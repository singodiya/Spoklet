import { AudioLines } from 'lucide-react';
export default function Loading() { return <div className="full-page-state" role="status"><AudioLines className="pulse" size={40}/><p>Finding your place in the conversation…</p></div>; }
